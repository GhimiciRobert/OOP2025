#pragma once
#include "Student.h"

int comparare_nume(student* student1, student* student2);
int comparare_nota_m(student* student1, student* student2);
int comparare_nota_e(student* student1, student* student2);
int comparare_nota_i(student* student1, student* student2);
int comparare_nota_finala(student* student1, student* student2);